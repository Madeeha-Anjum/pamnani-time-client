/** @type {import('next').NextConfig} */
console.log(
  'Welcome to Timmy Client, you are running in',
  process.env.NODE_ENV,
  'mode 🚀🚀🚀'
)
const nextConfig = {}
module.exports = nextConfig
